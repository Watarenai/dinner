# CLAUDE.md

## プロジェクト概要

**食材スロット** — 夕飯の献立に使う食材をスロットマシンでランダムに決めるブラウザアプリ。GitHub Pagesでデプロイするシングルページアプリケーション。

- **リポジトリ**: `Watarenai/dinner`
- **言語**: 日本語（UIテキスト・コミットメッセージ）
- **デプロイ先**: GitHub Pages（ルートの `index.html` を配信）

## ファイル構成

```
dinner/
├── index.html                 # メインアプリケーション（GitHub Pagesで配信）
├── slot-sound.mp3             # スロット回転時の効果音
├── CLAUDE.md                  # 本ファイル（AI向け開発ガイド）
└── docs/                      # ドキュメント類
    ├── requirements.md        # 要件定義書
    └── development-plan.md    # 開発計画書
```

## 技術スタック

- **素のHTML/CSS/JavaScript** — フレームワーク・ビルドツール・パッケージマネージャ不使用
- `package.json` なし、npm依存なし、バンドラなし
- 全コードをHTMLファイル内にインライン（`<style>` / `<script>`）で記述

## アーキテクチャ

### アプリ構造（index.html）

命令的なDOM操作パターンで構成:

1. **データ定義**: 食材配列を変数で定義（`vegIngredients`, `meatIngredients`）
2. **DOM構築**: `buildReel(i)` でシャッフルした食材を `<ul>` に投入（ループ用に2倍複製）
3. **アニメーション**: `setInterval` によるスクロール。`step`(5〜15px)・`delay`(20〜60ms) をリールごとにランダム化
4. **停止処理**: `.stopped` クラス付与 → CSS transitionで滑らかにスナップ
5. **結果表示**: 全リール停止後に `#result` に選ばれた食材を表示
6. **音声**: `<audio>` 要素（`loop=true`）で回転中に再生、全停止で一時停止
7. **レスポンシブ**: メディアクエリ（480px以下）でリール・ボタンを縮小

### 主要関数

| 関数 | 役割 |
|---|---|
| `shuffle(arr)` | Fisher-Yatesシャッフルで食材順をランダム化 |
| `buildReel(i)` | リール `i` のDOMを構築（シャッフル＋2倍複製） |
| `spinReel(i)` | リール `i` のインターバルアニメーション開始 |
| `stopReel(i)` | リール `i` を停止、CSS transitionでスナップ、全停止なら結果表示 |
| `stopAll()` | 全4リールを一斉停止 |
| `resetReels()` | 全リールを再構築して再スピン |
| `showResult()` | 停止位置から食材を取得して結果表示 |
| `getItemSize()` | レスポンシブ対応: 実際のアイテムサイズを取得 |

### リール構成

- リール1〜3（`reel1`〜`reel3`）: 野菜食材（19品目）— 緑ボーダー
- リール4（`reel4`）: 肉食材（3品目: 牛肉, 鶏肉, 豚肉）— 赤ボーダー
- アイテムサイズ: 100×100px（スマホ時80×80px）

### UI構成

- 野菜リール群と肉リールをカテゴリラベル付きで分離表示
- 各リール直下に個別ストップボタン配置
- 野菜=緑、肉=赤のカラーコード統一（ボーダー・ラベル・ボタン）

## 開発ワークフロー

### ビルド不要

`index.html` をブラウザで直接開くだけで動作確認可能。

```bash
# ローカルサーバーが必要な場合
python3 -m http.server 8000
# http://localhost:8000 を開く
```

### 変更手順

1. `index.html` を直接編集（HTML/CSS/JSすべてこの1ファイル）
2. ブラウザで動作確認
3. わかりやすいコミットメッセージでコミット

### テスト・リント・CI

自動テスト・リンター・CIパイプラインは未構成。ブラウザでの手動テストのみ。

## Git規約

- **デフォルトブランチ**: `main`（リモート） / `master`（ローカル）
- **コミットメッセージ**: 日本語・英語の混在OK。既存スタイルに従う:
  - 日本語: `初回コミット：プロトタイプ追加`
  - 英語: `Add index.html as the homepage`
  - 混在: `Update index.html: ランダム速度化コードを反映`
- **履歴**: リニア（マージコミットなし）

## デプロイ

**GitHub Pages** でリポジトリルートから配信。デフォルトブランチへのpushでPagesが自動リビルドされる。

## AI向けガイドライン

- ビルドツール不要のミニマルプロジェクト。明示的な指示がない限りパッケージマネージャ・バンドラ・フレームワークを導入しない
- 全コードはHTMLファイル内にインライン。リファクタリングの指示がない限りこのパターンを維持
- UIテキストは日本語。日本語を維持し、ユーザー向け文字列は日本語で記述
- コミットメッセージは日本語・英語どちらでもOK
- リールアイテムサイズはCSS・JSのスナップ処理に影響するレイアウト定数（PC: 100px、スマホ: 80px）。`getItemSize()` で実際のサイズを動的取得
- 食材追加時は適切な配列（`vegIngredients` or `meatIngredients`）を更新し、リール数・ボタンラベルの整合性を保つ
- 音声再生は `slot-sound.mp3` がHTMLファイルと同じディレクトリにある前提
